# Knowledge bases

Knowledge-base configuration updates are partial. Public URL and site sources
are supported; local file upload belongs to Console navigation. Check document
or task status before retrying. Retrying every document can consume substantial
model capacity and requires explicit confirmation.
