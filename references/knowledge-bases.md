# Knowledge bases

Knowledge-base configuration updates are partial. Public URL and site sources
are supported; a URL source requires `url`, while a site source requires `url`
or a non-empty `urls` list. Local file upload belongs to Console navigation.
Check document or task status before retrying. Retrying every document can
consume substantial model capacity and requires explicit confirmation.
