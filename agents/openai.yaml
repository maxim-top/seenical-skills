interface:
  display_name: "Seenical Console"
  short_description: "Manage Seenical Agents, knowledge, content, and sites"
  brand_color: "#5D51CD"
  default_prompt: "Use $seenical-console to inspect the relevant Seenical resource first, then apply only the changes I request."

policy:
  allow_implicit_invocation: true
