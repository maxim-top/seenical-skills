# AI plugins

List plugins and their functions before editing them. Plugin name and public
callback endpoint updates are separate from function metadata. The runtime
does not expose headers, authentication, environment variables, secrets, or
private callback addresses. Read binding relations before replacing a binding
list; an empty list explicitly clears the selected binding.
